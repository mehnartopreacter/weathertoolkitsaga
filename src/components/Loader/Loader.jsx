import { CircleLoading } from "react-loadingg";
import { LoaderWrapper } from "./Loader.styles";

export const Loader = () => {
  return (
    <LoaderWrapper>
      <CircleLoading />
    </LoaderWrapper>
  );
};
